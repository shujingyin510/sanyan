// 示例包：问候工具
// 安装("sample") 或复制到 packages/sample/ 后用 加载包("sample")

定义 问候 (名称) {
    输出(连接("你好, ", 名称, "!"))
}

定义 重复问候 (名称, 次数) {
    设 i = 0
    循环 (i < 次数) {
        问候(名称)
        i = i + 1
    }
}
