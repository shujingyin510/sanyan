// API 健康检测 — 三态聚合比 up/down 更有意义
// 证明：超时 ≠ 宕机，可能 标记不确定请求，Kleene 保证不确定性传播

输出（"=== API 三态健康检测系统 ==="）
输出（"痛点：HTTP 超时 ≠ 服务宕机"）
输出（"三言：用「可能」标记超时请求，Kleene 保证不确定性传播"）

// ── 模拟 HTTP 健康检查 ──
定义 读健康状态 （服务名） ｛
    设 随机值 ＝ 随机数（1， 10）
    若 （随机值 ＜＝ 5） ｛
        返回（真）
    ｝ 再若 （随机值 ＜＝ 8） ｛
        返回（可能）  // 超时 — 不等于宕机！
    ｝ 否则 ｛
        返回（假）   // 确实宕机
    ｝
｝

// ── 三态健康检测 ──
定义 三态系统健康 （服务列表） ｛
    设 在线数 ＝ 0
    设 超时数 ＝ 0
    设 宕机数 ＝ 0

    遍历 服务 在 服务列表 ｛
        设 状态 ＝ 读健康状态（服务）
        输出（连接（"  [检测] ", 服务, " → ", 状态））

        若 （状态 ＝＝ 真） ｛
            在线数 ＝ 在线数 ＋ 1
        ｝ 再若 （状态 ＝＝ 可能） ｛
            超时数 ＝ 超时数 ＋ 1
        ｝ 否则 ｛
            宕机数 ＝ 宕机数 ＋ 1
        ｝
    ｝

    输出（连接（"  汇总: 在线=", 在线数, " 超时=", 超时数, " 宕机=", 宕机数））

    // Kleene 三态聚合决策
    若 （宕机数 ＞ 0） ｛
        输出（"  → 综合判定：系统故障"）
        返回（假）
    ｝

    若 （超时数 ＝＝ 0） ｛
        输出（"  → 综合判定：系统正常"）
        返回（真）
    ｝

    若 （超时数 ＝＝ 表长（服务列表）） ｛
        输出（"  → 综合判定：网络可能中断（全部超时）"）
        返回（可能）
    ｝

    输出（"  → 综合判定：系统部分不确定"）
    返回（可能）
｝

输出（"")
输出（"--- 三态健康检测 ---"）
设 服务列表 ＝ 列表（"用户服务", "订单服务", "支付服务", "库存服务"）
设 三态结果 ＝ 三态系统健康（服务列表）
输出（连接（"三态最终判定: ", 三态结果））

输出（"")
输出（"=== 对比结论（诚实版）==="）
输出（"超时≠宕机是对的，但 Python 用 Enum 三态即可（见 health_check.py）"）
输出（"本例二值并不吃亏，代码量相当——不该宣称「二值做不到」"）
输出（"")
输出（"三言此处的额外优势仅在：用 且/或 表达聚合传播时写法更短"）
输出（"  一个超时 + 一个正常 = 不确定；一个宕机 + 任何 = 故障"）
输出（"  想真正展示三态价值，circuit_sim（内置 Kleene 代数）才是有力例子"）
