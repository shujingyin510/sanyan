// API 健康检测 — 三态聚合比 up/down 更有意义
// 证明：超时 ≠ 宕机，可能 标记不确定请求，Kleene 保证不确定性传播

输出（"=== API 三态健康检测系统 ==="）
输出（"痛点：HTTP 超时 ≠ 服务宕机"）
输出（"三言：用「可能」标记超时请求，Kleene 保证不确定性传播"）

// ── 模拟 HTTP 健康检查 ──
定义 读健康状态 （服务名） ｛
    设 随机值 ＝ 随机数（1， 10）
    若 （随机值 ＜＝ 5） ｛
        返回（真）
    ｝ 再若 （随机值 ＜＝ 8） ｛
        返回（可能）  // 超时 — 不等于宕机！
    ｝ 否则 ｛
        返回（假）   // 确实宕机
    ｝
｝

// ── 三态健康检测 ──
定义 三态系统健康 （服务列表） ｛
    设 在线数 ＝ 0
    设 超时数 ＝ 0
    设 宕机数 ＝ 0

    遍历 服务 在 服务列表 ｛
        设 状态 ＝ 读健康状态（服务）
        输出（连接（"  [检测] ", 服务, " → ", 状态））

        若 （状态 ＝＝ 真） ｛
            在线数 ＝ 在线数 ＋ 1
        ｝ 再若 （状态 ＝＝ 可能） ｛
            超时数 ＝ 超时数 ＋ 1
        ｝ 否则 ｛
            宕机数 ＝ 宕机数 ＋ 1
        ｝
    ｝

    输出（连接（"  汇总: 在线=", 在线数, " 超时=", 超时数, " 宕机=", 宕机数））

    // Kleene 三态聚合决策
    若 （宕机数 ＞ 0） ｛
        输出（"  → 综合判定：系统故障"）
        返回（假）
    ｝

    若 （超时数 ＝＝ 0） ｛
        输出（"  → 综合判定：系统正常"）
        返回（真）
    ｝

    若 （超时数 ＝＝ 表长（服务列表）） ｛
        输出（"  → 综合判定：网络可能中断（全部超时）"）
        返回（可能）
    ｝

    输出（"  → 综合判定：系统部分不确定"）
    返回（可能）
｝

输出（"")
输出（"--- 三态健康检测 ---"）
设 服务列表 ＝ 列表（"用户服务", "订单服务", "支付服务", "库存服务"）
设 三态结果 ＝ 三态系统健康（服务列表）
输出（连接（"三态最终判定: ", 三态结果））

输出（"")
输出（"=== 对比结论 ==="）
输出（"二值检测（up/down）只有两个输出，超时只能归到 down"）
输出（"三态检测用「可能」保留超时信息，与宕机区分"）
输出（"")
输出（"Kleene 传播："）
输出（"  一个超时的节点 + 一个正常的节点 = 系统状态不确定"）
输出（"  一个宕机的节点 + 任何节点 = 系统故障"）
输出（"  二值检测做不到这种区分"）
