// 智能家居三态决策演示
定义 晚安 () {
    置 灯 = 灭;
    置 窗帘 = 关;
    置 风扇 = 守;    // 守 = 保持当前状态
    输出("晚安，所有设备已就绪");
}

晚安();

// 模拟传感器检测
设 是否有人 = 可能;   // 传感器信号不稳，不确定
若 (是否有人 == 真) {
    输出("有人在，灯保持亮");
} 再若 (是否有人 == 可能) {
    输出("不确定有没有人，保持待机状态");
} 否则 {
    输出("没有人，关闭所有设备");
}

输出("");