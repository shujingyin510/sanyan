// 类型标注测试

定义 加法 (x: 数字, y: 数字) {
    返回(x + y)
}

定义 问候 (名: 字符串) {
    返回(连接("你好，", 名))
}

// 正确的类型调用
输出(连接("加法: ", 加法(1, 2)))
输出(连接("问候: ", 问候("世界")))

// 错误的类型调用（应该被捕获）
尝试 {
    设 result = 加法("a", 1)
    输出(result)
} 捕获 (错误) {
    输出("类型检查通过：错误的类型被正确拒绝")
}

输出("类型标注测试完成")
