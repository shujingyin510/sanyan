(定义 fizzbuzz(n)
  (遍历 i 1 n 
    (若(等于(取余 i 15)0) 
      (输出 "FizzBuzz")
      (若(等于(取余 i 3)0) 
        (输出 "Fizz")
        (若(等于(取余 i 5)0) 
          (输出 "Buzz")
          (输出 i)
        )
      )
    )
  )
)