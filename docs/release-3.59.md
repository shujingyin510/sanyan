# 三言 v3.59 — 内部冻结完成线

> 状态：进行中（批次 1/4）  
> 性质：**内部技术资产冻结**，非对外产品发布  
> 决策：[ADR-009](adr/009-Release-359-Scope.md) · [ADR-008](adr/008-Bootstrap-Not-Production.md) · [ADR-010](adr/010-Trinary-Interop-Boundary.md)

---

## 一句话

把「三态 + 约束 + 自举差分」从实现提升为：**可规格化、可测试、可审计、可冻结** 的技术资产，结束 Unreleased 漂移。

---

## 成功标准（Definition of Done）

| # | 标准 | 验收物 | 状态 |
|---|------|--------|------|
| 1 | 约束语义规格化 | `docs/constraint.md` 形式语义（作用域/组合/冲突/违规值/限时/允许）+ 正反边界测试 | 批次 2 |
| 2 | 三态互操作规范 | `docs/ternary-logic.md` 新增互操作章节 + 矩阵测试 | 批次 3 |
| 3 | 差分电池覆盖关键判定 | 不仅 stdout：约束判定、错误类别、三态值、信封「因」 | 批次 4 |
| 4 | 文档与版本冻结 | 本文件终稿 + CHANGELOG 铸 3.59 + 版本号一致 | 批次 4 |

**非成功标准（3.59 不看）**：用户、下载、论文、Agent 解冻、性能榜、商业指标。

---

## 范围内（IN）

- 约束规格化与边界测试（批次 2）
- 三态/布尔/异常/状态机互操作规范（批次 3，**只规格化不扩语义**，默认见 ADR-010）
- 差分电池扩展（批次 4）
- 版本号 3.59.0 与 release notes
- 已在 Unreleased 的工程债：拆仓扫尾、允许抑制、糖 AST/词法、Windows 种子（随 3.59 一并固）

## 范围外（OUT）— 暂停清单

| 项 | 备注 |
|----|------|
| UR / 论文 | 独立仓 |
| Agent 解冻 / 扩张 | sanyan-agent，冻结 |
| NASM L4 差分 | 需 nasm+Linux |
| 类型编译期检查 | 3.59 之后再选 |
| 增量 LSP | 同上 |
| 自举链升生产 | [ADR-008](adr/008-Bootstrap-Not-Production.md) |
| 对外发布 / 指标 / 商业化 | 兴趣线，无需求 |

---

## 批次与验收

### 批次 1 — 定义完成线 ✅（本文件）

| 产出 | 验收 |
|------|------|
| `docs/release-3.59.md` | 范围 IN/OUT、DoD、批次表 |
| 冻结清单（下表） | 与 roadmap Frozen/暂停一致 |
| ADR-008 / 009 / 010 | 状态 accepted |

### 批次 2 — 约束语义规格化

| 产出 | 验收 |
|------|------|
| 更新 `docs/constraint.md` | 四关键字形式语义、作用域、嵌套、冲突、违规值表示、限时、允许 |
| 正反边界测试 | 每个语义点至少 1 正 1 反（可扩 `test_capability_*`） |
| 与实现对齐 | 文档不写未实现行为；未实现处标「非 3.59」 |

### 批次 3 — 三态互操作规范

| 产出 | 验收 |
|------|------|
| `docs/ternary-logic.md` 新章节 | 真值表、传播、`若`/循环/函数/异常/布尔/NULL/状态机 |
| 矩阵测试 | 条件三路 + 异常×三态 + 约束×可能（D8） |
| 默认不扩语义 | 与 ADR-010 一致；要改语义则新 ADR |

### 批次 4 — 差分扩展与冻结

| 产出 | 验收 |
|------|------|
| 差分电池扩展 | 约束判定 / 错误类别 / 三态值 / 因（不限于 stdout） |
| 远程 CI | GitHub Actions 绿 |
| 铸 3.59.0 | `__version__`、README、manual、CHANGELOG 一致；Unreleased 清空 |

---

## 冻结清单（3.59 内不再改口）

| 冻结项 | 现状锚点 |
|--------|----------|
| 生产路径 = Python | ADR-008；sugar.san 浅 AST 可接受 |
| 约束五类能力 + 四关键字 + 限时 | `docs/constraint.md`；测试 54+ |
| `允许` 抑制语义 | tolerated 元通道 + D8；帧级 allow-maybe |
| C 种子全平台 | Linux syscall + Windows CRT |
| Agent 不在本仓 | `AGENT_MOVED.md` |
| 三态在 `若` 中可能→假 | ADR-010；运行时不变 |
| 版本叙事 | 3.59 冻结前可 Unreleased；冻结后必须有版本号 |

---

## 风险（批次 1 扫描）

| 风险 | 缓解 |
|------|------|
| 规格写超实现 | 批次 2/3 反向：先扫测试与代码，再写规格 |
| 批次间范围蠕变 | 暂停清单 + IN/OUT；新想法进 backlog 不进 3.59 |
| CI 与本地差分不一致 | 批次 4 以远程 CI 为准 |
| 文档双语漂移 | 3.59 以中文册子为准；英文只改版本号与指针 |

---

## 与 roadmap 关系

`docs/roadmap.md` Next 在批次进行期间只保留 3.59 批次；类型检查 / LSP / NASM 等移入「3.59 之后」。

---

*批次 1 起草：2026-09-10。后续每批次完成更新本文件状态列。*
