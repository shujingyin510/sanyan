# 三言 扩展路线图

> 已有 50 个三态 API，以下为后续扩展方向。按优先级排列。

---

## 一、运行时扩展

| 方向 | 说明 | 示例 |
|------|------|------|
| 文件 I/O | 三态读写、日志带信度 | `三态写("log", 三态(真,0.9))` |
| 网络请求 | HTTP 三态响应 | `三态请求(url)` → 三态(状态码, 网络信度) |
| 数据库 | 三态查询结果带信度 | `三态查询("SELECT ...")` |
| 缓存 | 带信度 LRU/TTL 缓存 | `三态缓存(key, 三态(v,c), ttl)` |

## 二、控制流增强

| 方向 | 说明 |
|------|------|
| `若3(条件) {真} {可能} {假}` | Kleene 三分支 |
| `匹配3(值) {模式1:...模式2:...}` | 三态模式匹配 |
| `早退(条件, 值)` | 带信度截断的提前返回 |
| `?(v).unwrap()` | 链式信度传播错误累积 |

## 三、并发与分布式

| 方向 | 说明 |
|------|------|
| 三态锁 | 信度感知互斥 |
| `并发{任务1, 任务2}→融合` | 异步并行融合 |
| 三态事件流 | 窗口聚合、滑动平均 |
| 节点间信度同步 | RPC 传播置信度 |

## 四、类型系统

| 方向 | 说明 |
|------|------|
| `三态集<T>` | 泛型容器 |
| `三态图<K,V>` | 图结构 |
| `结果(T) \| 错误(E)` | 标记联合类型 |
| `?.` 可选链 | 传播空信度 |
| 静态信度推断 | 编译期信度包络分析 |

## 五、机器学习

| 方向 | 说明 |
|------|------|
| 三态感知机 | 三态权重更新 |
| 三态决策树 | 分裂节点信度传播 |
| 贝叶斯网络 | 条件概率图传播 |
| 集成学习 | 多模型置信度融合 |

## 六、作用域隔离（实现级）

| 方向 | 说明 |
|------|------|
| 模块导入信度隔离 | `导入("m")` 内部中间信度不泄露到调用方 |
| 信念集持久化 | `信念写("mem.bin")` / `信念读("mem.bin")` |
| 推断最小化 | 只保留结论的信度链，中间推导不暴露 |

## 七、调试与可观测性

| 方向 | 说明 |
|------|------|
| 追踪可视化 | 信度变化火焰图 |
| 泄漏检测 | 高信度堆积告警 |
| 性能分析 | 置信度计算开销 |
| 基准测试 | 带信度 vs 不带信度性能对比 |

## 八、生态工具

| 方向 | 说明 |
|------|------|
| 包管理器 | 三态库生态 |
| REPL | 交互式三态 shell |
| IDE 插件 | VSCode 信度高亮 |
| 文档生成 | API 信度注解自动生成 |

## 九、安全与密码学

| 方向 | 说明 |
|------|------|
| 三态加密 | 信度感知加密 |
| 零知识证明 | 三态 ZKP |
| 安全多方计算 | 信度保密计算 |

## 十、硬件与近似计算

| 方向 | 说明 |
|------|------|
| 信度量化编码 | 1 字节存值+信度 ✅ |
| LLVM 降频/DVFS | 低信度路径降频 ✅ |
| 循环穿孔 | 低信度代码块跳过 |
| 神经形态芯片 | 三态硬件加速 |
