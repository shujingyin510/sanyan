# 三言 v3.20.0 内置命令速查表

| 命令       | 语法                                              | 说明                           |
| ---------- | ------------------------------------------------- | ------------------------------ |
| `设`       | `设 变量 = 值;`                                   | 定义变量                       |
| `输出`     | `输出(表达式);`                                   | 打印值                         |
| `输入`     | `输入("提示")`                                    | 读取用户输入                   |
| `若`       | `若 (条件) { ... }`                               | 条件分支                       |
| `循环`     | `循环 (条件) { ... }`                             | 条件循环                       |
| `遍历`     | `遍历 i 从 1 到 n { ... }`                       | 遍历循环                       |
| `遍历...在` | `遍历 元素 在 容器 { ... }`                      | 遍历容器元素                   |
| `跳出`     | `跳出;`                                           | 退出当前循环                   |
| `继续`     | `继续;`                                           | 跳过本次循环迭代               |
| `返回`     | `返回(值);`                                       | 函数内提前退出并返回值         |
| `定义`     | `定义 名 (参数) { ... }`                         | 自定义命令                     |
| `函数`     | `函数(参数) { ... }`                              | 匿名函数                       |
| `λ`        | `λ(参数) { ... }`                                 | 匿名函数简写（v3.7.1）        |
| `判`       | `判 值 { 真 {...} 可能 {...} 假 {...} }`         | 三态分支                       |
| `模板`     | `模板{文本${表达式}文本}`                         | 字符串插值                     |
| `置`       | `置 设备 = 状态;`                                 | 设置执行器/传感器              |
| `查`       | `查 设备;`                                        | 查询设备状态，显示中文状态词（开/守/关） |
| `读取`     | `读取(传感器)`                                    | S 表达式读取传感器别名（v3.7.1）        |
| `写入`     | `写入(设备, 状态)`                                | S 表达式设置设备别名（v3.7.1）         |
| `查询`     | `查询(设备)`                                      | S 表达式查询设备别名（v3.7.1）         |
| `读`       | `读 传感器;`                                      | 读取传感器值                   |
| `对`       | `对 对象 { ... }`                                 | 上下文操作                     |
| `加载`     | `加载("文件")`                                    | 加载模块                       |
| `导入`     | `导入("路径")`                                    | 模块隔离加载（不污染全局）     |
| `导出`     | `导出 名称1 名称2`                               | 模块显式导出（v3.7.1）        |
| `注册设备` | `注册设备 名称 为 类型`                           | 动态注册 IoT 设备（v3.7.1）   |
| `调试`     | `调试();`                                         | 打印所有变量和设备             |
| `列表`     | `列表(1,2,3)`                                     | 创建列表                       |
| `数组`     | `数组(长度)`                                      | 创建定长数组                   |
| `字典`     | `字典(k,v,...)`                                   | 创建字典                       |
| `映射`     | `映射(f, 容器)`                                   | 元素映射                       |
| `过滤`     | `过滤(p, 容器)`                                   | 元素过滤                       |
| `归并`     | `归并(f, 容器)`                                   | 累积归并                       |
| `绝对值`   | `绝对值(x)`                                       | 取绝对值                       |
| `平方根`   | `平方根(x)`                                       | 整数平方根                     |
| `最大值`   | `最大值(a,b,...)`                                 | 取最大值                       |
| `最小值`   | `最小值(a,b,...)`                                 | 取最小值                       |
| `随机数`   | `随机数(a,b)`                                     | 随机整数                       |
| `随机态`   | `随机态()`                                        | 随机三态                       |
| `连接`     | `连接(s1,s2,...)`                                 | 拼接字符串                     |
| `取长`     | `取长(s)`                                         | 字符串/数字长度                |
| `字列`     | `字列(s)`                                         | 字符串转字符列表               |
| `当前时间` | `当前时间()`                                      | Unix 时间戳（秒）              |
| `等待`     | `等待(秒)`                                        | 暂停执行                       |
| `读文件`   | `读文件("路径")`                                  | 读取文本文件所有内容           |
| `写文件`   | `写文件("路径", "内容")`                          | 写入文本文件                   |
| `是数字`   | `是数字(值)`                                      | 判断是否为三值整数             |
| `是字符串` | `是字符串(值)`                                    | 判断是否为字符串               |
| `字符串相等` | `字符串相等(串1, 串2)`                           | 比较两个字符串                 |
| `字符串包含` | `字符串包含(串, 子串)`                            | 检查字符串是否包含子串（v3.20） |
| `字典键列表` | `字典键列表(字典)`                                | 返回字典所有键列表（v3.20）    |
| `删除键`   | `删除键(字典, 键)`                                | 删除字典中指定键（v3.20）      |
| `尝试`     | `尝试 { ... } 捕获 (变量) { ... }`               | 异常处理（窄捕获）             |
| `做`       | `做 { ... }`                                      | 顺序执行块                     |
| `取`       | `取(容器, 索引)`                                  | 获取容器元素                   |
| `置元素`   | `置元素(容器, 索引, 值)`                          | 设置容器元素                   |
| `列表合`   | `列表合(a, b)`                                    | 拼接两个列表                   |
| `表长`     | `表长(lst)`                                       | 获取列表长度                   |
| `组长`     | `组长(arr)`                                       | 获取数组长度                   |
| `数组列`   | `数组列(arr)`                                     | 数组转列表                     |
| `取键`     | `取键(字典, 键)`                                  | 字典按键取值                   |
| `置键`     | `置键(字典, 键, 值)`                              | 字典设置键值                   |
| `含键`     | `含键(字典, 键)`                                  | 检查字典是否包含键             |
| `子串`     | `子串(s, 开始, 结束)`                             | 提取子字符串                   |
| `替换`     | `替换(s, 旧串, 新串)`                             | 字符串替换                     |
| `分割`     | `分割(s, 分隔符)`                                 | 字符串分割                     |
| `查找`     | `查找(s, 子串)`                                   | 字符串查找                     |
| `去空白`   | `去空白(s)`                                       | 去除两端空白                   |
| `大写`     | `大写(s)`                                         | 字符串转大写                   |
| `小写`     | `小写(s)`                                         | 字符串转小写                   |
| `前缀`     | `前缀(s, 前缀)`                                   | 检查字符串前缀                 |
| `后缀`     | `后缀(s, 后缀)`                                   | 检查字符串后缀                 |
| `排序`     | `排序(lst)`                                       | 列表排序                       |
| `反转`     | `反转(lst)`                                       | 列表反转                       |
| `包含`     | `包含(lst, 元素)`                                 | 检查列表包含                   |
| `去重`     | `去重(lst)`                                       | 列表去重                       |
| `切片`     | `切片(lst, 开始, 结束)`                           | 列表切片                       |
| `求和`     | `求和(lst)`                                       | 列表求和                       |
| `合并`     | `合并(lst, 分隔符)`                               | 列表合并为字符串               |
| `取位`     | `取位(数字, 位置)`                                | 取十进制某位数字               |
| `正弦`     | `正弦(x)`                                         | 正弦（弧度，高精度浮点）       |
| `余弦`     | `余弦(x)`                                         | 余弦（弧度，高精度浮点）       |
| `正切`     | `正切(x)`                                         | 正切（弧度，高精度浮点）       |
| `对数`     | `对数(x)`                                         | 自然对数（高精度浮点）         |
| `常用对数` | `常用对数(x)`                                     | 常用对数（高精度浮点）         |
| `向下取整` | `向下取整(x)`                                     | floor                          |
| `向上取整` | `向上取整(x)`                                     | ceil                           |
| `四舍五入` | `四舍五入(x)`                                     | round                          |
| `三进制`   | `三进制("+-0")`                                   | 三进制字符串转整数             |
| `同`       | `同(a, b)`                                        | 三态相等（三进制层面）         |
| `应用`     | `应用(f, args...)`                                | 应用函数到参数                 |
| `计数`     | `计数(lst, 元素)`                                 | 统计元素出现次数               |
| `转JSON`   | `转JSON(值)`                                      | 将值转为 JSON 字符串           |
| `解析JSON` | `解析JSON(字符串)`                                | 解析 JSON 字符串为值           |
| `安装`                   | `安装("包名")` or `安装("包名", "URL")`                    | 安装包，支持从索引自动下载                  |
| `包列表`                  | `包列表()`                                            | 列出已安装的包                        |
| `不等于`                  | `不等于(a, b)`                                        | 比较：a ≠ b                       |
| `不大于`                  | `不大于(a, b)`                                        | 比较：a ≤ b                       |
| `不小于`                  | `不小于(a, b)`                                        | 比较：a ≥ b                       |
| `乘`                    | `乘(a, b)`                                          | 乘法运算，可用 `a * b`                |
| `等于`                   | `等于(a, b)`                                         | 比较：a = b                       |
| `大于`                   | `大于(a, b)`                                         | 比较：a > b                       |
| `大于等于`                 | `大于等于(a, b)`                                       | 比较：a ≥ b                       |
| `加载包`                  | `加载包("包名")`                                        | 加载已安装的包作为模块                    |
| `减`                    | `减(a, b)`                                          | 减法运算，可用 `a - b`                |
| `且`                    | `且(a, b)`                                          | 三值逻辑与（tritwise AND）            |
| `或`                    | `或(a, b)`                                          | 三值逻辑或（tritwise OR）             |
| `幂`                    | `幂(a, b)`                                          | 幂运算 a^b                        |
| `小于`                   | `小于(a, b)`                                         | 比较：a < b                       |
| `小于等于`                 | `小于等于(a, b)`                                       | 比较：a ≤ b                       |
| `非`                    | `非(a)`                                             | 三值逻辑非（tritwise NOT）            |
| `加`                    | `加(a, b)`                                          | 加法 a + b                          |
| `进程号`                | `进程号()`                                          | 获取当前进程 PID                     |
| `除`                    | `除(a, b)`                                          | 除法 a / b                          |
| `余`                    | `余(a, b)`                                          | 取余 a % b                          |
| `从`                    | `从 开始 到 结束`                                   | 遍历循环的起始值                     |
| `到`                    | `从 开始 到 结束`                                   | 遍历循环的结束值                     |
| `在`                    | `在 容器`                                           | 遍历容器元素                         |
| `捕获`                  | `尝试 { ... } 捕获 (变量) { ... }`                  | 异常捕获（配合尝试）                 |
| `存在`                  | `存在(值)`                                          | 判断值是否存在                       |
| `当前路径`              | `当前路径()`                                        | 获取当前工作目录路径                 |
| `是文件`                | `是文件(路径)`                                      | 判断路径是否为文件                   |
| `是目录`                | `是目录(路径)`                                      | 判断路径是否为目录                   |
| `列出目录`              | `列出目录(路径)`                                    | 列出目录下的所有条目                 |
| `执行`                  | `执行(命令)`                                        | 执行系统命令                         |
| `环境变量`              | `环境变量(名称)`                                    | 获取环境变量值                       |
| `设环境变量`            | `设环境变量(名称, 值)`                              | 设置环境变量                         |
| `平台`                  | `平台()`                                            | 获取运行平台信息                     |
| `睡眠`                  | `睡眠(秒)`                                          | 暂停执行（同等待）                   |
| `延迟`                  | `延迟(秒, 函数)`                                    | 延迟后执行函数                       |
| `计时`                  | `计时()`                                            | 获取高精度计时器                     |
| `时间戳`                | `时间戳()`                                          | Unix 时间戳（毫秒）                  |
| `格式化时间`            | `格式化时间(格式字符串, 时间戳)`                    | 格式化时间戳为字符串                 |
| `并发`                  | `并发(f1, f2, ...)`                                 | 并发执行多个函数                     |
| `锁`                    | `锁()`                                              | 创建互斥锁                           |
| `锁住`                  | `锁住(锁对象)`                                      | 获取锁                               |
| `开锁`                  | `开锁(锁对象)`                                      | 释放锁                               |
| `随机`                  | `随机(lst)`                                         | 从列表中随机选一个元素               |
| `随机字节`              | `随机字节(长度)`                                    | 生成随机字节                         |
| `随机整数`              | `随机整数(a, b)`                                    | 随机整数（闭区间）                   |
| `乱序`                  | `乱序(lst)`                                         | 打乱列表顺序                         |
| `选取`                  | `选取(lst, n)`                                      | 从列表中随机选取 n 个元素            |
| `均值`                  | `均值(lst)`                                         | 计算列表均值                         |
| `中位数`                | `中位数(lst)`                                       | 计算列表中位数                       |
| `方差`                  | `方差(lst)`                                         | 计算列表方差                         |
| `标准差`                | `标准差(lst)`                                       | 计算列表标准差                       |
| `字符`                  | `字符(码点)`                                        | 码点转字符                           |
| `字符码`                | `字符码(字符)`                                      | 字符转 Unicode 码点                  |
| `正则匹配`              | `正则匹配(模式, 字符串)`                            | 正则匹配返回布尔值                   |
| `正则搜索`              | `正则搜索(模式, 字符串)`                            | 正则搜索第一个匹配                   |
| `正则查找`              | `正则查找(模式, 字符串)`                            | 正则查找所有匹配                     |
| `正则分割`              | `正则分割(模式, 字符串)`                            | 正则分割字符串                       |
| `正则替换`              | `正则替换(模式, 替换, 字符串)`                      | 正则替换字符串                       |
| `base64编码`            | `base64编码(字符串)`                                | Base64 编码                          |
| `base64解码`            | `base64解码(字符串)`                                | Base64 解码                          |
| `md5`                   | `md5(字符串)`                                       | 计算 MD5 哈希                        |
| `sha256`                | `sha256(字符串)`                                    | 计算 SHA256 哈希                     |
| `url编码`               | `url编码(字符串)`                                   | URL 编码                             |
| `url解码`               | `url解码(字符串)`                                   | URL 解码                             |
| `unicode编码`           | `unicode编码(字符串)`                               | Unicode 编码                         |
| `unicode解码`           | `unicode解码(字符串)`                               | Unicode 解码                         |
| `http读`                 | `http读(URL)`                                       | HTTP GET 请求                        |
| `http写`                 | `http写(URL, 数据)`                                 | HTTP POST 请求                       |
| `沙箱`                  | `沙箱()`                                            | 创建沙箱环境                         |
| `沙箱开`                | `沙箱开(沙箱)`                                      | 启动沙箱环境                         |
