／／ 集合与数据结构工具库
／／ 提供栈、队列、集合等常用数据结构

定义 栈 （） ｛
    ／／ 创建一个空栈
    返回（字典（"数据"， 列表（）， "类型"， "栈"））
｝

定义 压入 （栈， 值） ｛
    ／／ 将值压入栈顶
    设 数据 ＝ 取键（栈， "数据"）
    列表合（数据， 列表（值））
    置键（栈， "数据"， 数据）
    返回（真）
｝

定义 弹出 （栈） ｛
    ／／ 弹出栈顶元素
    设 数据 ＝ 取键（栈， "数据"）
    若 （表长（数据） ＝＝ 0） ｛
        返回（空）
    ｝
    设 最后索引 ＝ 表长（数据） － 1
    设 值 ＝ 取（数据， 最后索引）
    设 新数据 ＝ 切片（数据， 0， 最后索引）
    置键（栈， "数据"， 新数据）
    返回（值）
｝

定义 栈大小 （栈） ｛
    ／／ 获取栈中元素数量
    返回（表长（取键（栈， "数据"）））
｝

定义 栈空 （栈） ｛
    ／／ 检查栈是否为空
    返回（栈大小（栈） ＝＝ 0）
｝

定义 队列 （） ｛
    ／／ 创建一个空队列
    返回（字典（"数据"， 列表（）， "类型"， "队列"））
｝

定义 入队 （队列， 值） ｛
    ／／ 将值加入队列末尾
    设 数据 ＝ 取键（队列， "数据"）
    列表合（数据， 列表（值））
    置键（队列， "数据"， 数据）
    返回（真）
｝

定义 出队 （队列） ｛
    ／／ 从队列头部取出元素
    设 数据 ＝ 取键（队列， "数据"）
    若 （表长（数据） ＝＝ 0） ｛
        返回（空）
    ｝
    设 值 ＝ 取（数据， 0）
    设 新数据 ＝ 切片（数据， 1）
    置键（队列， "数据"， 新数据）
    返回（值）
｝

定义 队列大小 （队列） ｛
    ／／ 获取队列中元素数量
    返回（表长（取键（队列， "数据"）））
｝

定义 队列空 （队列） ｛
    ／／ 检查队列是否为空
    返回（队列大小（队列） ＝＝ 0）
｝

定义 集合创建 （） ｛
    ／／ 创建一个空集合
    返回（字典（））
｝

定义 集合添加 （集合， 元素） ｛
    ／／ 向集合中添加元素
    置键（集合， 字符串（元素）， 真）
    返回（真）
｝

定义 集合包含 （集合， 元素） ｛
    ／／ 检查集合是否包含元素
    返回（含键（集合， 字符串（元素）））
｝

定义 集合删除 （集合， 元素） ｛
    ／／ 从集合中删除元素
    若 （含键（集合， 字符串（元素））） ｛
        置键（集合， 字符串（元素）， 空）
        返回（真）
    ｝
    返回（假）
｝

定义 集合大小 （集合） ｛
    ／／ 获取集合中元素数量
    设 计数 ＝ 0
    遍历 键 在 字列（集合） ｛
        若 （取键（集合， 键） ！＝ 空） ｛
            计数 ＝ 计数 ＋ 1
        ｝
    ｝
    返回（计数）
｝

导出 栈
导出 压入
导出 弹出
导出 栈大小
导出 栈空
导出 队列
导出 入队
导出 出队
导出 队列大小
导出 队列空
导出 集合创建
导出 集合添加
导出 集合包含
导出 集合删除
导出 集合大小
