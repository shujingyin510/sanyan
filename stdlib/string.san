／／ 字符串工具库
定义 重复 （s， n） ｛
    设 result ＝ ""
    循环 （n ＞ 0） ｛
        result ＝ 连接（result， s）
        n ＝ n － 1
    ｝
    返回（result）
｝

定义 是否包含 （字符串， 子串） ｛
    设 mainLen ＝ 取长（字符串）
    设 subLen ＝ 取长（子串）
    若 （subLen ＞ mainLen） ｛
        返回（假）
    ｝
    遍历 i 从 0 到 （mainLen － subLen） ｛
        设 match ＝ 真
        遍历 j 从 0 到 （subLen － 1） ｛
            若 （取（字列（字符串）， i ＋ j） 不等于 取（字列（子串）， j）） ｛
                match ＝ 假
                返回（假）
            ｝
        ｝
        若 （match 等于 真） ｛
            返回（真）
        ｝
    ｝
    返回（假）
｝

定义 替换 （字符串， 旧词， 新词） ｛
    设 result ＝ ""
    设 mainLen ＝ 取长（字符串）
    设 oldLen ＝ 取长（旧词）
    设 i ＝ 0
    循环 （i ＜ mainLen） ｛
        若 （且（i ＋ oldLen ＜＝ mainLen， 字符串相等（子串（字符串， i， oldLen）， 旧词））） ｛
            result ＝ 连接（result， 新词）
            i ＝ i ＋ oldLen
        ｝ 否则 ｛
            result ＝ 连接（result， 取（字列（字符串）， i））
            i ＝ i ＋ 1
        ｝
    ｝
    返回（result）
｝

定义 子串 （字符串， 开始， 长度） ｛
    设 buf ＝ ""
    遍历 j 从 0 到 （长度 － 1） ｛
        buf ＝ 连接（buf， 取（字列（字符串）， 开始 ＋ j））
    ｝
    返回（buf）
｝

定义 反转 （s） ｛
    设 result ＝ ""
    设 len ＝ 取长（s）
    遍历 i 从 1 到 len ｛
        result ＝ 连接（result， 取（字列（s）， len － i））
    ｝
    返回（result）
｝

定义 修剪 （s） ｛
    返回（s）
｝