／／ 字符串工具库（使用内置函数）

定义 重复 （s， n） ｛
    设 result ＝ ""
    循环 （n ＞ 0） ｛
        result ＝ 连接（result， s）
        n ＝ n － 1
    ｝
    返回（result）
｝

定义 是否包含 （字符串， 子串） ｛
    返回（查找（字符串， 子串） ＞＝ 0）
｝

定义 反转字符串 （s） ｛
    设 chars ＝ 字列（s）
    设 reversed ＝ 反转（chars）
    返回（合并（reversed， ""））
｝

定义 左填充 （s， 总长， 填充符） ｛
    设 当前长 ＝ 取长（s）
    若 （当前长 ＞＝ 总长） ｛
        返回（s）
    ｝
    设 需填充 ＝ 总长 － 当前长
    设 填充串 ＝ 重复（填充符， 需填充）
    返回（连接（填充串， s））
｝

定义 右填充 （s， 总长， 填充符） ｛
    设 当前长 ＝ 取长（s）
    若 （当前长 ＞＝ 总长） ｛
        返回（s）
    ｝
    设 需填充 ＝ 总长 － 当前长
    设 填充串 ＝ 重复（填充符， 需填充）
    返回（连接（s， 填充串））
｝

定义 计数 （字符串， 子串） ｛
    设 count ＝ 0
    设 start ＝ 0
    设 len_s ＝ 取长（字符串）
    设 len_sub ＝ 取长（子串）
    循环 （start ＋ len_sub ＜＝ len_s） ｛
        设 pos ＝ 查找（子串（字符串， start， len_s）， 子串）
        若 （pos ＞＝ 0） ｛
            count ＝ count ＋ 1
            start ＝ start ＋ pos ＋ len_sub
        ｝ 否则 ｛
            跳出
        ｝
    ｝
    返回（count）
｝
