// sanyan-cc.san — 自举编译器
#include "sugar.san"
#include "llvmgen.san"

// 读取输入文件 → 解析 → 编译 → 输出 LLVM IR
// 用法: 将输入路径替换到 san_read_file 的第一个参数
设 content = san_read_file("build/_test_in.san", 0)
若 (等于(content, 0)) {
    输出("error")
} 否则 {
    设 ast = 解析(content)
    设 ir = 编译顶层(ast)
    输出(ir)
}
