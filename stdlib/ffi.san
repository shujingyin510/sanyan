// 外语互操作·库层糖（stdlib/ffi.san）⚗️ 实验性
//
// 把「每个外调都要包一层 解包」的样板收进一批便利函数。语义统一为
// 解包(py算子(...))：成功返回载荷、失败即抛（与 导入 的失败语义一致）。
// 想按三态优雅处理外部失败（超时/异常分流），仍请直接用裸算子：
//     匹配3(信封判(py调(f, x))) { 真 → ..., 假 → ..., 可能 → ... }
//
// 用法（模块别名访问）：
//     导入 "stdlib/ffi.san" 为 ffi
//     设 j = ffi.库("json")
//     输出(ffi.方法1(j, "dumps", 字典("a", 1)))   // {"a": 1}
//
// 前置：SANYAN_FFI=1（未开时这些函数在调用点抛「FFI 未启用」可读错误）。
// 注：固定参数个数（py调 是变长算子，库函数按 0-3 参覆盖绝大多数用法）；
// 更多参数或需要三态分流时回到裸算子。

定义 库 （名） ｛
    // 导入 Python 模块并解包 —— 等价 解包(py导入(名))
    返回（解包（py导入（名）））
｝

定义 成员 （句柄， 名） ｛
    // 取属性并解包 —— 等价 解包(py取(句柄, 名))
    返回（解包（py取（句柄， 名）））
｝

定义 元素 （句柄， 键） ｛
    // 下标取值并解包 —— 等价 解包(py项(句柄, 键))
    返回（解包（py项（句柄， 键）））
｝

定义 调用0 （可调） ｛
    // 调用可调对象并解包 —— 等价 解包(py调(可调))
    返回（解包（py调（可调）））
｝

定义 调用1 （可调， 甲） ｛
    返回（解包（py调（可调， 甲）））
｝

定义 调用2 （可调， 甲， 乙） ｛
    返回（解包（py调（可调， 甲， 乙）））
｝

定义 调用3 （可调， 甲， 乙， 丙） ｛
    返回（解包（py调（可调， 甲， 乙， 丙）））
｝

定义 方法0 （对象， 名） ｛
    // 取方法并调用（obj.名()）一步到位 —— 等价 解包(py调(解包(py取(对象, 名))))
    返回（解包（py调（解包（py取（对象， 名）））））
｝

定义 方法1 （对象， 名， 甲） ｛
    返回（解包（py调（解包（py取（对象， 名））， 甲）））
｝

定义 方法2 （对象， 名， 甲， 乙） ｛
    返回（解包（py调（解包（py取（对象， 名））， 甲， 乙）））
｝

导出 库
导出 成员
导出 元素
导出 调用0
导出 调用1
导出 调用2
导出 调用3
导出 方法0
导出 方法1
导出 方法2
